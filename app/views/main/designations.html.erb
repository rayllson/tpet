<% @designations.each do |designation| %>
	<% #if !designation.localization.nil? %>
		Local: <%= link_to designation.localization.name, designation.localization %></br> 
		Dia: <%= designation.week_day %></br>
		Horário: <%= designation.schedule.strftime("%H:%M") %></br>
		Companheiros: 
		<% designation.users.map do |user|%>
			<%= link_to user.name, user %>,
		<% end %>
	<% #end %>
	<br><br>
<% end %> 